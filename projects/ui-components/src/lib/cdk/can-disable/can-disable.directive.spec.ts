import { CanDisableDirective } from './can-disable.directive';

describe('DisabledStateDirective', () => {
  it('should create an instance', () => {
    const directive = new CanDisableDirective();
    expect(directive).toBeTruthy();
  });
});
